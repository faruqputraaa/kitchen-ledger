# docker/Dockerfile.client.simple
FROM node:20-alpine AS builder
WORKDIR /app
COPY client/package*.json ./
RUN npm ci
COPY client/ ./
RUN npm run build
# Output di /app/dist - di-mount ke host via volume
